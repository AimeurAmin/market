import styles from './student-slots.module.scss'

const { container } = styles;

const StudentSlots = () => {
  return (
    <div className={container}>StudentSlots</div>
  )
}

export default StudentSlots