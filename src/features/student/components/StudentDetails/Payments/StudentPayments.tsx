import styles from './student-payments.module.scss';

const { container } = styles;

const StudentPayments = () => {
  return (
    <div className={container}>StudentPayments</div>
  )
}

export default StudentPayments